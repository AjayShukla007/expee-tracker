'use client'

import React from 'react'

const AddToCart = () => {
    return (
        <div>
            <button onClick={() => console.log('add to card clicked')
            }>ADDTOCART</button>
        </div>
    )
}

export default AddToCart